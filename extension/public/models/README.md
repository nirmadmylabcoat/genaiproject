# Browser-Side Model Artifacts

Place ONNX-converted YOLOv8 weights (e.g., `yolov8-ui.onnx`) and tokenizer assets in this directory.
Files are exposed to the extension via the `web_accessible_resources` manifest entry.

